# Copy to `.env` and fill in your real key (`.env` is git-ignored).
# Used only by the local data-updater scripts, not by the Jekyll build.
# Get your key at https://www.last.fm/api/accounts
LASTFM_API_KEY=your_key_here
LASTFM_USER=isabel-mp3
